{
  "dependencies": {
    "@babel/cli": "^7.1.2",
    "@babel/core": "^7.1.2",
    "@babel/preset-es2015": "^7.0.0-beta.53",
    "babel-plugin-monadic-do": "^0.0.2",
    "crocks": "^0.10.2",
    "webpack": "^4.5.0"
  },
  "scripts": {
    "build": "babel src/*.js --out-dir lib",
    "hmm": "babel src/*.js --out-dir lib; && node src/"
  }
}
